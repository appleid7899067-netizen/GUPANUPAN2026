# storage — per-project FS isolation
- Puter: `puter.fs` แยก `/<username>/AppData/<appID>/<projectId>/`
- Totalum: `/api/v1/vcaas/projects/<id>/files` (hosted)
- ดูไฟล์ใน Workspace → Tab Code หรือ Database
